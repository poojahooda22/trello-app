# Copy to .env. Bun loads this file and inlines these two into the client
# bundle, so the browser learns where the API and the relay live.
# Docker sets them as real environment variables instead, which take priority.
BUN_PUBLIC_API_URL=http://localhost:3001
BUN_PUBLIC_WS_URL=ws://localhost:3002
