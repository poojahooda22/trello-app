# Repo-wiki log

> Append-only, newest at top. Parseable prefix `## [YYYY-MM-DD] <op> | <title>` where `<op>` is
> `ingest`, `lint`, or `query`, so `grep "^## \[" log.md | head` gives a timeline.
> One entry per operation, followed by a `Touched:` line listing the pages.

## [2026-08-23] init | repo-wiki scaffolded
Touched: WIKI.md, index.md, log.md, glossary.md.
